name: tavolara
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0-261.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  web: ^1.1.1
  freezed_annotation: ^3.1.0
  flutter_colorpicker: ^1.1.0
  flex_color_picker: ^3.8.0
  google_fonts: ^8.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  freezed: ^3.2.5
  build_runner: ^2.13.1

flutter:
  uses-material-design: true
